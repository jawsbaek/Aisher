# Test environment configuration for Docker-based integration tests
# This file is used by tests/test_integration_docker.py

# ClickHouse connection (Docker container)
CLICKHOUSE_HOST=localhost
CLICKHOUSE_PORT=8123
CLICKHOUSE_USER=default
CLICKHOUSE_PASSWORD=test_password
CLICKHOUSE_DATABASE=signoz_traces

# LLM configuration (optional for most tests)
LLM_MODEL=gpt-4-turbo
OPENAI_API_KEY=sk-test-key-not-used-in-integration-tests

# Performance settings
QUERY_TIMEOUT=30
LLM_TIMEOUT=45
MAX_RETRIES=3
STACK_MAX_LENGTH=600
